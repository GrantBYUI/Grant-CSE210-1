using System;

public Class product
{
    get product ("Display product")
    get customer name("Display Customer name");
    get product ID ("Display Product ID");
    get price   ("Display Price");
    get quantity   ("Display Quantity");
    get display() GetDisplay (Display Product);

    return product;
    return cost;
    
    // Contains the name, product id, price, and quantity of each product.
    // The total cost of this product is computed by multiplying the price per unit and the quantity. (If the price per unit was $3 and they bought 5 of them, the product total cost would be $15.)
}